{
  "quick_test": {
    "description": "Quick test run (5 minutes)",
    "epochs": 3,
    "batch_size": 8,
    "train_samples": 500,
    "val_samples": 100,
    "learning_rate": 5e-4,
    "skip_distance": 3,
    "skip_alpha": 0.5
  },
  "baseline_comparison": {
    "description": "Standard baseline comparison (30 minutes)",
    "epochs": 10,
    "batch_size": 16,
    "train_samples": 5000,
    "val_samples": 1000,
    "learning_rate": 5e-4,
    "skip_distance": 3,
    "skip_alpha": 0.5
  },
  "deep_exploration": {
    "description": "Extended training for publication (2-3 hours)",
    "epochs": 30,
    "batch_size": 16,
    "train_samples": 20000,
    "val_samples": 2000,
    "learning_rate": 3e-4,
    "skip_distance": 3,
    "skip_alpha": 0.5
  },
  "skip_distance_sweep": {
    "description": "Test multiple skip distances",
    "epochs": 10,
    "batch_size": 16,
    "train_samples": 5000,
    "val_samples": 1000,
    "learning_rate": 5e-4,
    "skip_distances": [2, 3, 4, 5],
    "skip_alpha": 0.5
  },
  "skip_alpha_sweep": {
    "description": "Test multiple skip weights",
    "epochs": 10,
    "batch_size": 16,
    "train_samples": 5000,
    "val_samples": 1000,
    "learning_rate": 5e-4,
    "skip_distance": 3,
    "skip_alphas": [0.3, 0.5, 0.7, 0.9]
  }
}
